@model IEnumerable<PF_Pach_OS.Models.Proveedore>
<link href="~/css/proveedores.css" rel="stylesheet" />

@{
    ViewData["Title"] = "Index";
}


@*<p>
    <a asp-action="Create">Create New</a>
</p>
*@
<!-- Modal -->
<div id="myModal" class="modal fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display:none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">X</button>
            </div>
            <div class="modal-body">
                <div id="modal-content">
                    Cargando....
                </div>
            </div>
        </div>
    </div>
</div>


<button id="btnNuevo" class="estilo-btn-naranja btn-crear-posicion">Crear Proveedor</button>
<script>
    document.getElementById("btnNuevo").addEventListener("click", function () {
        $('#myModal').modal('show');
    });
</script>


<div class=" d-flex lista-proveedores flex-wrap justify-content-between">
    @foreach (var item in Model)
    {
        <article class="card card__content">
            <header>
            </header>
            <main class="card__body">
                <h1 class="title--primary">@Html.DisplayFor(modelItem => item.NomLocal)</h1>
                <span>@Html.DisplayFor(modelItem => item.Telefono)</span>
                <div class="d-flex pt-2 justify-content-around">
                    <a id="btnEditar" data-id="@item.IdProveedor" data-toggle="modal" class="cursor-pointer">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="blue" class="bi bi-pen-fill" viewBox="0 0 16 16">
                            <path d="m13.498.795.149-.149a1.207 1.207 0 1 1 1.707 1.708l-.149.148a1.5 1.5 0 0 1-.059 2.059L4.854 14.854a.5.5 0 0 1-.233.131l-4 1a.5.5 0 0 1-.606-.606l1-4a.5.5 0 0 1 .131-.232l9.642-9.642a.5.5 0 0 0-.642.056L6.854 4.854a.5.5 0 1 1-.708-.708L9.44.854A1.5 1.5 0 0 1 11.5.796a1.5 1.5 0 0 1 1.998-.001z" />
                        </svg>
                    </a>
                    <script>
                        document.getElementById("btnEditar").addEventListener("click", function () {
                            $('#myModal').modal('show');
                        });
                    </script>
                    @* <a id="btnDetails" data-id="@item.IdProveedor" data-toggle="modal">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="orange" class="bi bi-info-circle-fill" viewBox="0 0 16 16">
                            <path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.293.006-.399-.287-.47l-.451-.081.082-.381 2.29-.287zM8 5.5a1 1 0 1 1 0-2 1 1 0 0 1 0 2z" />
                        </svg>
                    </a>
                    <script>
                        document.getElementById("btnDetails").addEventListener("click", function () {
                            $('#myModal').modal('show');
                        });
                    </script> *@
                    @if (item.Estado == 0)
                    {
                        <form asp-action="Disable" asp-route-id="@item.IdProveedor" method="post" class="align-self-end">
                            <button type="submit" class="btn btn-link">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="red" class="bi bi-circle-fill" viewBox="0 0 16 16">
                                    <circle cx="8" cy="8" r="8" />
                                </svg>
                            </button>
                        </form>
                    }
                    else
                    {
                        <form asp-action="Enable" asp-route-id="@item.IdProveedor" method="post" class="align-self-end">
                            <button type="submit" class="btn btn-link">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="green" class="bi bi-circle-fill" viewBox="0 0 16 16">
                                    <circle cx="8" cy="8" r="8" />
                                </svg>
                            </button>
                        </form>
                    }
                </div>
            </main>
           
        </article>
    }
</div>
@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomLocal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Correo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomLocal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Correo)
            </td>
            <td>
            <td>
                @if (item.Estado == 0)
                {
                <form asp-action="Disable" asp-route-id="@item.IdProveedor" method="post">
                <button type="submit" class="btn btn-danger">Deshabilitado</button>
                </form>
                }
                else
                {
                <form asp-action="Enable" asp-route-id="@item.IdProveedor" method="post">
                <button type="submit" class="btn btn-success">Habilitado</button>
                </form>
                }
            </td>
            <td>
                    <a id="btnEditar" class="btn btn-primary btn-lg" data-id="@item.IdProveedor" data-toggle="modal">Editar</a>
                    <script>
                        document.getElementById("btnEditar").addEventListener("click", function () {
                            $('#myModal').modal('show');
                        });
                    </script>
                    <a id="btnDetails" class="btn btn-primary btn-lg" data-id="@item.IdProveedor" data-toggle="modal">Detalles</a>
                    <script>
                        document.getElementById("btnDetails").addEventListener("click", function () {
                            $('#myModal').modal('show');
                        });
                    </script>

            </td>
        </tr>
}
    </tbody>
</table> *@
@section scripts
{
    <script src="~/js/proveedores/proveedore.js"></script>
}
